const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // I might not have this, I should use a different way to query DB.
// Let's check if there's a script I can run
